@model WebMyWorldEC.Models.Payment

@{
    ViewBag.Title = "Delete Payment";
}

@if (Request.IsAuthenticated)
{
    <h2>@ViewBag.Title.</h2>
    <div>
        <h3>Are you sure you want to delete this?</h3>
        <div class="form-group">
            <div>
                <input class="btn btn-default" value="Back to List" type="submit" onclick="location.href='@Url.Action("Index", "Payments")';return false;" />
            </div>
        </div>
    </div>
    <div>
        <hr />
        <dl class="dl-horizontal col-md-offset-3">
            <dt>
                @Html.DisplayNameFor(model => model.Cost)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Cost)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.PaymentDate)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.PaymentDate)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Entertainment_Centers.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Entertainment_Centers.Name)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.User.FirstName)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.User.FirstName)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Service.Name)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Service.Name)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions col-md-offset-5">
                <input type="submit" value="Delete" class="btn btn-default" />
            </div>
        }
    </div>
    
}
else
{
    Response.Redirect(Url.Action("Login", "Account"));
}